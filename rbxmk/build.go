package main

// Prerelease specifies a prerelease version. If not empty, the displayed
// version is appended with "-<prerelease>"
var Prerelease string

// Build specifies build metadata. If not empty, the displayed version is
// appended with "+<build>"
var Build string
